-- Readable snapshot of the current D1 schema. Apply numbered files in
-- `db/d1-migrations/` (see wrangler.jsonc). Table definitions live in
-- `db/d1-schema.sql` and `src/lib/db-schema.ts`. Do not run this file at runtime.

PRAGMA foreign_keys = ON;
